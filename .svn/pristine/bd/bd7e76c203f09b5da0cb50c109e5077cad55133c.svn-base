using System;
using System.Linq;
using System.Windows.Forms;

namespace ObligatorioIS2
{
    public partial class BuscarProducto : Form
    {
        public BuscarProducto()
        {
            InitializeComponent();
        }

        private void button1_Click(object sender, EventArgs e)
        {
            Hide();
            var venMenuPpal = new MenuPpal();
            venMenuPpal.Show();
        }

        private void button2_Click(object sender, EventArgs e)
        {
            Producto productoBuscado = null;

            if (radioButton1.Checked)
            {
                productoBuscado = Sistema.GetInstance()
                    .ListaProductos
                    .FirstOrDefault(p => p.Nombre.Contains(textBox1.Text));
            }
            else if (radioButton2.Checked)
            {
                productoBuscado = Sistema.GetInstance()
                    .ListaProductos
                    .FirstOrDefault(p => p.Codigo.Contains(textBox1.Text));
            }
            else if (radioButton3.Checked)
            {
                productoBuscado = Sistema.GetInstance()
                    .ListaProductos
                    .FirstOrDefault(p => p.Rubro.Contains(textBox1.Text));
            }
            else if (radioButton4.Checked)
            {
                productoBuscado = Sistema.GetInstance()
                    .ListaProductos
                    .FirstOrDefault(p => p.Tipo.Contains(textBox1.Text));
            }
            
            Hide();
            var venDetalleProd = new DetalleDeProducto(productoBuscado);
            venDetalleProd.Show();
        }
    }
}
